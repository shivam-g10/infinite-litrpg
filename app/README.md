# App Placeholder

Next.js workspace package belongs here. Do not create nested Git repository. Use `src/app/` for Next App Router so package path stays unambiguous.

Bootstrap requirements:

- TypeScript strict mode.
- Server-side OpenAI calls only.
- Root `.env` loading.
- Reader mode and God Mode inspector.
- Local SQLite persistence.
- Vitest and Playwright.

Replace this file with app-specific setup notes during Phase 1.
